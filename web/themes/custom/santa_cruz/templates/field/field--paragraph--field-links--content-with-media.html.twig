{% for item in items %}
  <a href="{{ item.content['#url'] }}"
     class="btn-link"
     target="{{ item.content['#url'].isExternal() ? '_blank' : '' }}">{{ item.content['#title'] }}</a>
{% endfor %}
